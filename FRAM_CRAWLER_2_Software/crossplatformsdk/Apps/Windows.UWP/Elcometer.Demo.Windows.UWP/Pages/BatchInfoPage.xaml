<Page
    x:Class="Elcometer.Demo.Windows.UWP.Pages.BatchInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Elcometer.Demo.Windows.UWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="1" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button
            x:Name="BackButton"
            Grid.Row="0"
            Style="{StaticResource NavigationBackButtonNormalStyle}" />

        <Rectangle Grid.Row="1" Fill="LightGray" />

        <ScrollViewer x:Name="LiveScrollViewer" Grid.Row="2">
            <TextBlock
                x:Name="BatchInfoTextBlock"
                Margin="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top" />

        </ScrollViewer>
    </Grid>
</Page>
